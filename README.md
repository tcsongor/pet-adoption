## Pet adoption

To initialize the database, run:

`composer create-database`

To reset the database to the default seeded values:

`composer recreate-database`

To launch the server:

`php artisan serve`

Login is possible with the two test accounts found in database\seeders\DatabaseSeeder.php